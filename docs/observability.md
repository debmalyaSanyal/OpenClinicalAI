# Observability

Included hooks:

- Structured JSON logging
- PHI redaction filter
- Health endpoints
- Prometheus service scaffold
- Performance benchmark location in `evaluation/`

Future modules should expose metrics without emitting PHI.
